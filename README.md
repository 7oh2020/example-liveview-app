# Example LiveView App

This is a example social networking app developed with LiveView.

## Setup

```bash
mix setup
mix phx.server
```

Now you can visit [`localhost:4000`](http://localhost:4000) from your browser.
